[project]
name = "learn-claude-code-openai-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.20.0",
    "python-dotenv>=1.2.1",
]

[project.optional-dependencies]
logfire = [
    "logfire",
]
